<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ViewCustom">
        <item name="android:layout_width"> match_parent</item>
        <item name="android:layout_height"> wrap_content</item>
        <item name="android:layout_margin"> 8dp</item>
    </style>

    <style name="NumberPickerTheme" parent="Theme.AppCompat.DayNight">
        <!-- Color del texto principal -->
        <item name="android:textColorPrimary">@color/wii_blue</item>
        <!-- Eliminar líneas divisorias -->
        <item name="colorControlNormal">@android:color/transparent</item>
        <!-- Tamaño del texto -->
        <item name="android:textSize">30sp</item>
        <!-- Estilo del texto -->
        <item name="android:textStyle">normal</item>
    </style>

    <style name="CustomFABStyle" parent="Widget.Material3.FloatingActionButton.Primary">
        <item name="shapeAppearance">@style/ShapeAppearance.Material3.Corner.Full</item>
        <item name="backgroundTint">@color/fab_background_light</item>
        <item name="tint">@color/fab_icon_light</item>
    </style>

</resources>